# playground

Run the playground from the monorepo root with `yarn dev`.
See the root [README](../../README.md) for setup.

**Live demo (GitHub Pages):** https://uncharted-flatware.github.io/jsonforms-primevue-monorepo/

Build for Pages locally: `yarn build:pages` from this workspace (uses base path `/jsonforms-primevue-monorepo/`).